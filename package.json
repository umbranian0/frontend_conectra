{
  "name": "connecttroca-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Base frontend for the ConnectTroca mobile prototype",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:host": "vite --host 0.0.0.0 --port 5173",
    "build": "node --max-old-space-size=256 ./node_modules/vite/bin/vite.js build --minify=false",
    "preview": "vite preview",
    "preview:host": "vite preview --host 0.0.0.0 --port 4173",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:up:full": "docker compose -f docker-compose.yml -f ../backend_conectra/docker-compose.yml up --build",
    "docker:down:full": "docker compose -f docker-compose.yml -f ../backend_conectra/docker-compose.yml down"
  },
  "keywords": [
    "react",
    "vite",
    "prototype",
    "frontend"
  ],
  "dependencies": {
    "lucide-react": "^0.511.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.7.0",
    "vite": "^5.4.21"
  }
}
